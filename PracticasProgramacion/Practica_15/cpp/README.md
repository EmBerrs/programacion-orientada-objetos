# Practica_15 - Factorial y Fibonacci Recursivos

## Objetivo
Implementar factorial y fibonacci de forma recursiva.

## Diagrama de Clases

```
+--------------+
| Recursividad |
+--------------+
| + factorial()|
| + fibonacci()|
+--------------+
```

## Compilación

```bash
mkdir build && cd build && cmake .. && cmake --build .
./practica_15
```
