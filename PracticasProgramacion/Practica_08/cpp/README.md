# Practica_08 - Identificar Tamaños de Tipos

## Objetivo
Mostrar el tamaño (sizeof) de tipos básicos y tipos creados (estructura PE y clase POO).

## Diagrama de Clases

```
+----------------------+
| IdentificadorTamanios|
+----------------------+
| + mostrarTamanios... |
+----------------------+
```

## Compilación

```bash
mkdir build && cd build && cmake .. && cmake --build .
./practica_08
```
