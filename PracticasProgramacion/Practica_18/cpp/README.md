# Practica_18 - Merge Sort

## Objetivo
Ordenar caracteres, enteros y tipo creado (PE y POO) con Merge Sort.

## Compilación

```bash
mkdir build && cd build && cmake .. && cmake --build .
./practica_18
```
