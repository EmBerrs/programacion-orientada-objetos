# Practica_20 - Burbuja Ordenamiento Indirecto

## Objetivo
Ordenar caracteres, enteros y tipo creado mediante punteros (acceso indirecto).

## Compilación

```bash
mkdir build && cd build && cmake .. && cmake --build .
./practica_20
```
